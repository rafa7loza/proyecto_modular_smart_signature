import dashboard from './dashboard';
import administration from './administration';
import signature from './signature';

const menuItems = {
  items: [dashboard, signature, administration]
};

export default menuItems;
